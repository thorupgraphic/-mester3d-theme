<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ page_title }}</title>
  {{ 'style.css' | asset_url | stylesheet_tag }}
  {{ content_for_header }}
</head>
<body>
  <header>
    <a href="/" class="logo">🖨️ Mester3D</a>
    <nav>
      <a href="/">Hjem</a>
      <a href="/collections/all">Alle produkter</a>
      <a href="/cart">Kurv</a>
    </nav>
  </header>

  <main id="MainContent">
    {{ content_for_layout }}
  </main>

  <footer>
    <p><strong>Mester3D</strong> — 3D-printede produkter med passion</p>
    <p>© {{ 'now' | date: '%Y' }} Mester3D. Alle rettigheder forbeholdt.</p>
  </footer>
</body>
</html>
